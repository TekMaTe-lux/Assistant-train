(()=>{
'use strict';
const API='https://vps.labetaillere.fr';
const FILES={routes:'data/routes.txt',stops:'data/stops.txt',trips:'data/trips.txt',stopTimes:'data/stop_times.txt',calendarDates:'data/calendar_dates.txt'};
const FRANCE_BOUNDS=L.latLngBounds([[41.20,-5.70],[51.35,9.85]]);
const filters={ter:true,tgv:true,intercites:true,transilien:true,autres:true};
const routeMap=new Map(),stopMap=new Map(),tripMeta=new Map(),infraCache=new Map();
let schedules=[],runs=[],currentPositions=[],selectedRunKey=null,selectedTime=null,liveTimer=null,midnightKey='';
let baseLayer=null,infraAbort=null,infraTimer=null,infraSerial=0;
const statusEl=document.getElementById('status'),timeEl=document.getElementById('mapTime'),liveBtn=document.getElementById('liveBtn'),baseMapEl=document.getElementById('baseMap');
const detailEl=document.getElementById('detail'),detailTitleEl=document.getElementById('detailTitle'),detailMetaEl=document.getElementById('detailMeta'),detailSubEl=document.getElementById('detailSub'),stopsEl=document.getElementById('stops');
const map=L.map('map',{preferCanvas:true,zoomControl:false,attributionControl:false,fadeAnimation:false,zoomAnimation:true,markerZoomAnimation:false});
map.fitBounds(FRANCE_BOUNDS,{padding:[8,8]});
L.control.zoom({position:'bottomleft'}).addTo(map);
L.control.attribution({position:'bottomright',prefix:false}).addTo(map).addAttribution('© OpenStreetMap · SNCF Réseau · horaires GTFS théoriques');
const tiles={
standard:()=>L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,updateWhenIdle:true,keepBuffer:2}),
faded:()=>L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,updateWhenIdle:true,keepBuffer:2}),
bw:()=>L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,updateWhenIdle:true,keepBuffer:2}),
satellite:()=>L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{maxZoom:19,updateWhenIdle:true,keepBuffer:2}),
'sat-faded':()=>L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{maxZoom:19,updateWhenIdle:true,keepBuffer:2})};
const infrastructureLayer=L.geoJSON(null,{renderer:L.canvas({padding:.35}),interactive:false,style:infraStyle}).addTo(map);
const trainLayer=L.layerGroup().addTo(map);
function setStatus(text){statusEl.textContent=text}
function setBaseMap(mode){if(!tiles[mode])mode='standard';if(baseLayer)map.removeLayer(baseLayer);document.body.classList.remove('mode-faded','mode-bw','mode-sat-faded');if(mode==='faded')document.body.classList.add('mode-faded');if(mode==='bw')document.body.classList.add('mode-bw');if(mode==='sat-faded')document.body.classList.add('mode-sat-faded');baseLayer=tiles[mode]().addTo(map);baseLayer.bringToBack();try{localStorage.setItem('lb-france-basemap',mode)}catch(_){}}
function infraStyle(feature){const p=feature?.properties||{},kind=String(p.kind||'classic').toLowerCase(),source=String(p.source||'').toUpperCase();if(source==='CFL')return{color:'#e83e8c',weight:1.9,opacity:.72};if(kind==='lgv')return{color:'#7457e8',weight:2.4,opacity:.84};if(kind==='connector')return{color:'#ff9f1c',weight:1.6,opacity:.78,dashArray:'5 5'};if(kind==='closed')return{color:'#727b86',weight:1,opacity:.35,dashArray:'4 5'};return{color:'#00a9c7',weight:1.45,opacity:.58}}
function bboxString(){const b=map.getBounds();return[b.getWest(),b.getSouth(),b.getEast(),b.getNorth()].map(v=>v.toFixed(5)).join(',')}
function infraKey(){const b=map.getBounds(),z=Math.max(6,Math.min(11,map.getZoom()));return`${z}|${[b.getWest(),b.getSouth(),b.getEast(),b.getNorth()].map(v=>v.toFixed(2)).join(',')}`}
async function refreshInfrastructure(){if(map.getZoom()<6){infrastructureLayer.clearLayers();return}const key=infraKey();if(infraCache.has(key)){infrastructureLayer.clearLayers().addData(infraCache.get(key));return}const serial=++infraSerial;if(infraAbort)infraAbort.abort();infraAbort=new AbortController();try{const r=await fetch(`${API}/api/map-v2/infrastructure?bbox=${bboxString()}`,{signal:infraAbort.signal,headers:{Accept:'application/json'}});if(!r.ok)throw new Error(String(r.status));const data=await r.json();if(serial!==infraSerial)return;infraCache.set(key,data);if(infraCache.size>6)infraCache.delete(infraCache.keys().next().value);infrastructureLayer.clearLayers().addData(data)}catch(e){if(e?.name!=='AbortError')console.warn('[france] infrastructure',e)}}
function scheduleInfrastructure(delay=450){clearTimeout(infraTimer);infraTimer=setTimeout(refreshInfrastructure,delay)}
function csvLine(line){const out=[];let cur='',quoted=false;for(let i=0;i<line.length;i++){const ch=line[i];if(quoted){if(ch==='"'){if(line[i+1]==='"'){cur+='"';i++}else quoted=false}else cur+=ch}else if(ch==='"')quoted=true;else if(ch===','){out.push(cur);cur=''}else cur+=ch}out.push(cur);return out}
async function streamCsv(url,onRow){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`${url}: HTTP ${r.status}`);if(!r.body){const text=await r.text();let head=null;for(const raw of text.split(/\r?\n/)){if(!raw)continue;const row=csvLine(raw);if(!head){head=Object.fromEntries(row.map((x,i)=>[x.replace(/^\uFEFF/,''),i]));continue}onRow(row,head)}return}const reader=r.body.getReader(),dec=new TextDecoder();let buf='',head=null;for(;;){const{value,done}=await reader.read();buf+=dec.decode(value||new Uint8Array(),{stream:!done});let nl;while((nl=buf.indexOf('\n'))>=0){let line=buf.slice(0,nl);buf=buf.slice(nl+1);if(line.endsWith('\r'))line=line.slice(0,-1);if(!line)continue;const row=csvLine(line);if(!head){head=Object.fromEntries(row.map((x,i)=>[x.replace(/^\uFEFF/,''),i]));continue}onRow(row,head)}if(done)break}if(buf.trim()){const row=csvLine(buf.replace(/\r$/,''));if(!head)head=Object.fromEntries(row.map((x,i)=>[x.replace(/^\uFEFF/,''),i]));else onRow(row,head)}}
function field(row,h,name){const i=h[name];return i==null?'':(row[i]??'')}
function dateKey(date){return`${date.getFullYear()}${String(date.getMonth()+1).padStart(2,'0')}${String(date.getDate()).padStart(2,'0')}`}
function localTimeValue(date=new Date()){return`${String(date.getHours()).padStart(2,'0')}:${String(date.getMinutes()).padStart(2,'0')}`}
function timeToSec(value){const m=/^(\d{1,2}):(\d{2})(?::(\d{2}))?$/.exec(String(value||'').trim());return m?(Number(m[1])*3600+Number(m[2])*60+Number(m[3]||0)):null}
function formatSec(sec){if(!Number.isFinite(sec))return'';const h=Math.floor(sec/3600),m=Math.floor((sec%3600)/60);return`${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}`}
function extractTrainNumber(...parts){const text=parts.filter(Boolean).join(' '),matches=[...text.matchAll(/(?:^|\D)(\d{4,6})(?!\d)/g)];return matches.length?matches[matches.length-1][1]:''}
function isRailRoute(route){if(!route)return true;const n=Number(route.type);return!Number.isFinite(n)||n===2||(n>=100&&n<200)}
function categoryFor(meta,route){const t=`${route?.short||''} ${route?.long||''} ${route?.desc||''} ${meta.headsign||''} ${meta.shortName||''} ${meta.tripId||''}`.toUpperCase();if(/TGV|INOUI|OUIGO|EUROSTAR|THALYS/.test(t))return'tgv';if(/INTERCIT/.test(t))return'intercites';if(/TRANSILIEN|\bRER\b/.test(t))return'transilien';if(/\bTER\b/.test(t))return'ter';return'autres'}
function categoryColor(cat){return cat==='tgv'?'#a76cff':cat==='intercites'?'#ffb347':cat==='transilien'?'#58d68d':cat==='ter'?'#00b9d8':'#b8c2cc'}
async function loadStaticGtfs(){document.body.classList.add('is-loading');routeMap.clear();stopMap.clear();tripMeta.clear();schedules=[];runs=[];const now=new Date(),yesterday=new Date(now);yesterday.setDate(now.getDate()-1);const todayKey=dateKey(now),yesterdayKey=dateKey(yesterday);midnightKey=todayKey;const serviceOffsets=new Map();setStatus('GTFS 1/5 · calendrier…');await Promise.all([
streamCsv(FILES.routes,(row,h)=>{const id=field(row,h,'route_id');if(!id)return;routeMap.set(id,{short:field(row,h,'route_short_name'),long:field(row,h,'route_long_name'),desc:field(row,h,'route_desc'),type:field(row,h,'route_type')})}),
streamCsv(FILES.stops,(row,h)=>{const id=field(row,h,'stop_id');if(!id)return;const lat=Number(field(row,h,'stop_lat')),lon=Number(field(row,h,'stop_lon'));if(Number.isFinite(lat)&&Number.isFinite(lon))stopMap.set(id,{name:field(row,h,'stop_name')||id,lat,lon})}),
streamCsv(FILES.calendarDates,(row,h)=>{const sid=field(row,h,'service_id'),d=field(row,h,'date'),ex=field(row,h,'exception_type');if(!sid||(d!==todayKey&&d!==yesterdayKey))return;const offset=d===todayKey?0:-1;let set=serviceOffsets.get(sid);if(!set){set=new Set();serviceOffsets.set(sid,set)}if(ex==='2')set.delete(offset);else set.add(offset)})]);
setStatus('GTFS 2/5 · trajets actifs…');await streamCsv(FILES.trips,(row,h)=>{const sid=field(row,h,'service_id'),offsetSet=serviceOffsets.get(sid);if(!offsetSet||!offsetSet.size)return;const routeId=field(row,h,'route_id'),route=routeMap.get(routeId);if(!isRailRoute(route))return;const tripId=field(row,h,'trip_id');if(!tripId)return;const meta={tripId,serviceId:sid,routeId,headsign:field(row,h,'trip_headsign'),shortName:field(row,h,'trip_short_name'),offsets:[...offsetSet]};meta.category=categoryFor(meta,route);meta.number=meta.shortName||extractTrainNumber(meta.tripId,meta.headsign,route?.short,route?.long)||'Train';tripMeta.set(tripId,meta)});
setStatus(`GTFS 3/5 · horaires (${tripMeta.size.toLocaleString('fr-FR')} trajets)…`);const grouped=new Map();await streamCsv(FILES.stopTimes,(row,h)=>{const tripId=field(row,h,'trip_id');if(!tripMeta.has(tripId))return;const stopId=field(row,h,'stop_id'),stop=stopMap.get(stopId);if(!stop)return;const arr=timeToSec(field(row,h,'arrival_time')),dep=timeToSec(field(row,h,'departure_time')),seq=Number(field(row,h,'stop_sequence'));let list=grouped.get(tripId);if(!list){list=[];grouped.set(tripId,list)}list.push({stopId,name:stop.name,lat:stop.lat,lon:stop.lon,arr,dep,seq:Number.isFinite(seq)?seq:list.length})});
setStatus('GTFS 4/5 · construction des circulations…');for(const[tripId,stops]of grouped){if(stops.length<2)continue;stops.sort((a,b)=>a.seq-b.seq);const meta=tripMeta.get(tripId),first=stops[0],last=stops[stops.length-1],firstSec=Number.isFinite(first.dep)?first.dep:first.arr,lastSec=Number.isFinite(last.arr)?last.arr:last.dep;if(!Number.isFinite(firstSec)||!Number.isFinite(lastSec)||lastSec<firstSec)continue;const sched={...meta,stops,firstSec,lastSec,origin:first.name,destination:last.name,route:routeMap.get(meta.routeId)||null};schedules.push(sched);for(const offset of meta.offsets)runs.push({key:`${tripId}|${offset}`,schedule:sched,offset,start:firstSec+offset*86400,end:lastSec+offset*86400})}document.body.classList.remove('is-loading');setStatus(`GTFS prêt · ${schedules.length.toLocaleString('fr-FR')} circulations du jour`);recomputePositions();refreshInfrastructure()}
function stopEventWindow(stop){const arr=Number.isFinite(stop.arr)?stop.arr:stop.dep,dep=Number.isFinite(stop.dep)?stop.dep:stop.arr;return{arr,dep}}
function positionForRun(run,targetAbs){const s=run.schedule,rel=targetAbs-run.offset*86400,st=s.stops;if(rel<s.firstSec||rel>s.lastSec)return null;for(let i=0;i<st.length;i++){const w=stopEventWindow(st[i]);if(Number.isFinite(w.arr)&&Number.isFinite(w.dep)&&rel>=w.arr&&rel<=w.dep)return{lat:st[i].lat,lon:st[i].lon,index:i,progress:0,rel}}for(let i=0;i<st.length-1;i++){const a=st[i],b=st[i+1],wa=stopEventWindow(a),wb=stopEventWindow(b),t1=wa.dep,t2=wb.arr;if(!Number.isFinite(t1)||!Number.isFinite(t2)||t2<=t1)continue;if(rel>=t1&&rel<=t2){const p=Math.max(0,Math.min(1,(rel-t1)/(t2-t1)));return{lat:a.lat+(b.lat-a.lat)*p,lon:a.lon+(b.lon-a.lon)*p,index:i,progress:p,rel}}}const last=st[st.length-1];return{lat:last.lat,lon:last.lon,index:st.length-1,progress:1,rel}}
function activeTargetSeconds(){const value=selectedTime||localTimeValue(),[h,m]=value.split(':').map(Number);return h*3600+m*60+(selectedTime?0:new Date().getSeconds())}
function recomputePositions(){if(!runs.length)return;const today=dateKey(new Date());if(today!==midnightKey&&!selectedTime){loadStaticGtfs().catch(showError);return}const target=activeTargetSeconds(),out=[];for(const run of runs){if(target<run.start||target>run.end)continue;const pos=positionForRun(run,target);if(!pos)continue;out.push({runKey:run.key,run,schedule:run.schedule,lat:pos.lat,lon:pos.lon,index:pos.index,progress:pos.progress,category:run.schedule.category,number:run.schedule.number})}currentPositions=out;renderTrains();const shown=out.filter(p=>filters[p.category]!==false).length;setStatus(`${shown.toLocaleString('fr-FR')} trains théoriques · ${selectedTime||localTimeValue()} · GTFS statique`);if(selectedRunKey)refreshSelectedDetail()}
function escapeHtml(value){return String(value??'').replace(/[&<>'"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch]))}
function labelIcon(p){return L.divIcon({className:'train-icon',html:`<span class="train-marker ${escapeHtml(p.category)}" data-run="${escapeHtml(p.runKey)}"><span class="num">${escapeHtml(p.number)}</span></span>`,iconSize:[1,1],iconAnchor:[0,0]})}
function visibleBoundsPadded(){return map.getBounds().pad(.18)}
function renderTrains(){trainLayer.clearLayers();const mode=map.getZoom()<8?'dot':'label',bounds=visibleBoundsPadded();for(const p of currentPositions){if(filters[p.category]===false||!bounds.contains([p.lat,p.lon]))continue;let marker;if(mode==='dot'){const c=categoryColor(p.category);marker=L.circleMarker([p.lat,p.lon],{renderer:L.canvas(),radius:2.7,color:c,weight:1,fillColor:c,fillOpacity:.88,opacity:.95,bubblingMouseEvents:false})}else marker=L.marker([p.lat,p.lon],{icon:labelIcon(p),keyboard:false,riseOnHover:true,bubblingMouseEvents:false});marker.on('click',e=>{L.DomEvent.stopPropagation(e);selectedRunKey=p.runKey;showDetail(p)});marker.addTo(trainLayer)}}
function showDetail(p){const s=p.schedule;detailTitleEl.textContent=`${s.category.toUpperCase()} ${s.number}`;detailMetaEl.textContent=`${s.origin} → ${s.destination}`;const next=s.stops[Math.min(s.stops.length-1,p.index+1)];detailSubEl.textContent=next?`Position théorique GTFS · vers ${next.name}`:'Position théorique GTFS';stopsEl.innerHTML=s.stops.map((stop,i)=>{const w=stopEventWindow(stop),t=Number.isFinite(w.dep)?w.dep:w.arr;return`<li class="${i===p.index||i===p.index+1?'current':''}"><span class="stop-time">${escapeHtml(formatSec(t))}</span><span class="stop-name">${escapeHtml(stop.name)}</span></li>`}).join('');detailEl.hidden=false}
function refreshSelectedDetail(){if(!selectedRunKey)return;const p=currentPositions.find(x=>x.runKey===selectedRunKey);if(p)showDetail(p);else clearSelection()}
function clearSelection(){selectedRunKey=null;detailEl.hidden=true}
function showError(error){console.error('[france]',error);document.body.classList.remove('is-loading');setStatus(`Erreur GTFS · ${error?.message||error}`)}
document.querySelectorAll('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{const k=btn.dataset.filter;filters[k]=!filters[k];btn.classList.toggle('is-active',filters[k]);recomputePositions()}));
document.getElementById('allBtn').addEventListener('click',()=>{for(const k of Object.keys(filters))filters[k]=true;document.querySelectorAll('[data-filter]').forEach(b=>b.classList.add('is-active'));recomputePositions()});
document.getElementById('franceBtn').addEventListener('click',()=>map.fitBounds(FRANCE_BOUNDS,{padding:[8,8]}));
document.getElementById('closeDetail').addEventListener('click',clearSelection);
baseMapEl.addEventListener('change',()=>setBaseMap(baseMapEl.value));
timeEl.value=localTimeValue();
timeEl.addEventListener('change',()=>{selectedTime=timeEl.value||null;liveBtn.classList.toggle('is-active',!selectedTime);recomputePositions()});
liveBtn.addEventListener('click',()=>{selectedTime=null;timeEl.value=localTimeValue();liveBtn.classList.add('is-active');recomputePositions()});
map.on('moveend',()=>{renderTrains();scheduleInfrastructure(420)});
map.on('zoomend',()=>{renderTrains();scheduleInfrastructure(220)});
map.on('click',clearSelection);
document.addEventListener('visibilitychange',()=>{if(!document.hidden&&!selectedTime){timeEl.value=localTimeValue();recomputePositions()}});
let savedBase='standard';try{savedBase=localStorage.getItem('lb-france-basemap')||'standard'}catch(_){ }if(!tiles[savedBase])savedBase='standard';baseMapEl.value=savedBase;setBaseMap(savedBase);
clearInterval(liveTimer);liveTimer=setInterval(()=>{if(!document.hidden&&!selectedTime){timeEl.value=localTimeValue();recomputePositions()}},15000);
loadStaticGtfs().catch(showError);
})();