--- a/map-v2/public/carte-core-preview.html
+++ b/map-v2/public/carte-core-preview.html
@@ -1146,6 +1146,44 @@
     calendar_dates: 'calendar_datescfl.txt'
   };
 
+  // Points d'exploitation frontaliers du GTFS CFL : utiles au tracé des
+  // marches, mais jamais présentés comme des arrêts voyageurs.
+  const CFL_TECHNICAL_BORDER_STOP_IDS = new Set([
+    '000300000131', // Arlon, Sterpenich-Frontière
+    '000300000034', // Rodange-Frontière (Athus)
+    '000400000053', // Rodange-Gr.
+    '000400000064', // Bettembourg-frontière
+    '000300000033', // Troisvierges-Frontière
+    '000500000081'  // Wasserbillig-Frontière
+  ]);
+
+  function isCflTechnicalBorderStopId(value){
+    const raw = String(value || '').replace(/^CFL:/i,'').trim();
+    return CFL_TECHNICAL_BORDER_STOP_IDS.has(raw);
+  }
+
+  // Aux frontières, HAFAS peut publier l'annulation du tronçon technique ou
+  // d'un numéro partenaire (IC/SNCB, TER, DB) sous le numéro CFL. Une telle
+  // valeur ne doit pas supprimer la gare voyageur voisine. Les retards restent
+  // utilisés et une autre source temps réel peut toujours confirmer l'annulation.
+  const HAFAS_BORDER_CANCELLATION_GUARD = new Set([
+    'arlon','athus','gouvy','troisvierges','wasserbillig','trier hbf','trier',
+    'perl','audun-le-tiche','audun le tiche','volmerange-les-mines','volmerange les mines','longwy'
+  ].map(normalizeStationName));
+
+  function isLikelyHafasBorderCancellationArtifact(stopMeta, entry){
+    if (!entry || entry.value != null) return false;
+    if (normalizeRealtimeSourceKey(entry.sourceKey || entry.source) !== 'hafas') return false;
+    const values = [stopMeta?.name, stopMeta?.stop_name, entry.original]
+      .filter(Boolean)
+      .map(normalizeStationName);
+    return values.some(name =>
+      HAFAS_BORDER_CANCELLATION_GUARD.has(name)
+      || /(?:^|[ ,\/-])(?:frontiere|grenze)(?:[ ,\/-]|$)/.test(name)
+      || /(?:^|[- ])gr(?:\.| |$)/.test(name)
+    );
+  }
+
   const CFL_ROUTE_IDS_ALLOWED = new Set(['302','300','299','297']); // pas de TER 
 
   // ---------- Helpers fetch multi-bases ----------
@@ -635,6 +668,7 @@
   function shouldIgnoreHafasCancellationForStop(trainContext, stopMeta, entry){
     if (!entry || entry.value != null) return false;
     if (normalizeRealtimeSourceKey(entry.sourceKey || entry.source) !== 'hafas') return false;
+    if (isLikelyHafasBorderCancellationArtifact(stopMeta, entry)) return true;
     if (!hasSncfRealtimeAffinity(trainContext)) return false;
     const network = classifyStopRealtimeNetwork(stopMeta);
     return network !== 'cfl';
@@ -11001,11 +11035,16 @@
 
       const stopTimeRows = await parseCSVAny(CFL_FILES.stop_times, CFL_BASES);
       const cflStopTimesByTrip = new Map();
+      let ignoredTechnicalBorderStopTimes = 0;
       for (const row of stopTimeRows){
         const rawTripId = (row.trip_id || '').trim();
         const meta = cflTripsMeta.get(rawTripId);
         if (!meta) continue;
         const rawStopId = (row.stop_id || '').trim();
+        if (isCflTechnicalBorderStopId(rawStopId)){
+          ignoredTechnicalBorderStopTimes++;
+          continue;
+        }
         const stopKey = stopIdMap.get(rawStopId);
         if (!stopKey) continue;
         if (!cflStopTimesByTrip.has(rawTripId)) cflStopTimesByTrip.set(rawTripId, []);
@@ -11100,7 +11139,10 @@
         }
       }
 
-      return { addedStops, addedTrips };
+      if (ignoredTechnicalBorderStopTimes){
+        console.info(`[CFL] ${ignoredTechnicalBorderStopTimes} passages par points frontière techniques ignorés`);
+      }
+      return { addedStops, addedTrips, ignoredTechnicalBorderStopTimes };
     } catch(e){
       console.warn('[CFL] Chargement GTFS CFL impossible', e);
       return null;
