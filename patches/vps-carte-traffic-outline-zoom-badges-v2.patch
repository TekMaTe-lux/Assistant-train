diff --git a/map-v2/public/carte-core-preview.html b/map-v2/public/carte-core-preview.html
index 271b8dd..176c926 100644
--- a/map-v2/public/carte-core-preview.html
+++ b/map-v2/public/carte-core-preview.html
@@ -8769,6 +8769,11 @@ function berDrawSelectedRouteProgress(){
     if (train.branding?.className) classNames.push(train.branding.className);
     const isCancelled = trainFilters.showDelays && delayInfo?.severity === 'cancelled';
     if (isCancelled) classNames.push('train-cancelled');
+    if (trainFilters.showDelays && delayInfo){
+      classNames.push(isCancelled ? 'ber-traffic-cancelled' : 'ber-traffic-delayed');
+    } else {
+      classNames.push('ber-traffic-ontime');
+    }
     if (activeTripId && String(train.id) === String(activeTripId)) {
       classNames.push('train-selected');
     }
@@ -21048,4 +21053,48 @@ html body.ber-marker-arrow-mode
 </style>
 <!-- BER CLEAN TRAIN SHEET + COLORED ARROWS V1 END -->
 
+<!-- BER TRAFFIC OUTLINE + ZOOM BADGES V2 START -->
+<style id="ber-traffic-outline-zoom-badges-v2">
+/* L'état du train se lit d'abord sur le contour de la flèche : cette
+   information reste nette même lorsque la carte est très dézoomée. */
+html body.ber-marker-arrow-mode .leaflet-marker-pane .cow-marker {
+  --ber-train-status-color:var(--ber-live-train-color,#22e6ff);
+}
+html body.ber-marker-arrow-mode .leaflet-marker-pane .cow-marker.ber-traffic-delayed {
+  --ber-train-status-color:#ff7a00;
+}
+html body.ber-marker-arrow-mode .leaflet-marker-pane .cow-marker.ber-traffic-cancelled,
+html body.ber-marker-arrow-mode .leaflet-marker-pane .cow-marker.train-cancelled {
+  --ber-train-status-color:#ff1744;
+}
+html body.ber-marker-arrow-mode
+.leaflet-marker-pane .cow-marker .ber-train-arrow-white {
+  stroke:var(--ber-train-status-color) !important;
+  stroke-width:var(--ber-train-white-width) !important;
+  stroke-opacity:1 !important;
+}
+html body.ber-marker-arrow-mode
+.leaflet-marker-pane .cow-marker .ber-train-arrow-svg {
+  filter:
+    drop-shadow(0 1px 0 var(--ber-train-status-color))
+    drop-shadow(0 3px 1px rgba(0,4,9,.96))
+    !important;
+}
+
+/* Zoom régional : aucun badge ni pastille. Le contour coloré suffit. */
+html.ber-arrow-z-far body .leaflet-marker-pane .cow-marker .train-delay-badge,
+html.ber-arrow-z-wide body .leaflet-marker-pane .cow-marker .train-delay-badge {
+  display:none !important;
+}
+
+/* Zoom axe, ville et proximité : badge trafic compact réactivé. */
+html.ber-arrow-z-axis body .leaflet-marker-pane .cow-marker .train-delay-badge,
+html.ber-arrow-z-city body .leaflet-marker-pane .cow-marker .train-delay-badge,
+html.ber-arrow-z-near body .leaflet-marker-pane .cow-marker .train-delay-badge,
+html.ber-arrow-z-close body .leaflet-marker-pane .cow-marker .train-delay-badge {
+  display:inline-flex !important;
+}
+</style>
+<!-- BER TRAFFIC OUTLINE + ZOOM BADGES V2 END -->
+
 </html>
