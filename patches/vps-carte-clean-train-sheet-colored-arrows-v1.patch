diff --git a/map-v2/public/carte-core-preview.html b/map-v2/public/carte-core-preview.html
index c2c329154..271b8dde7 100644
--- a/map-v2/public/carte-core-preview.html
+++ b/map-v2/public/carte-core-preview.html
@@ -9436,7 +9436,10 @@ const mapContainerEl = map.getContainer();
       const note = noteParts.length ? `<div class="stop-note"><strong>${escapeHTML(noteParts.join(' · '))}</strong></div>` : '';
       const timesHtml = info.timeParts.map(part=>{
         const cls = part.type === 'arr' ? 'time-arr' : part.type === 'dep' ? 'time-dep' : 'time-neutral';
-        const pieces = [];
+        const kindLabel = part.type === 'arr' ? 'A' : part.type === 'dep' ? 'D' : '';
+        const pieces = [
+          `<span class="time-entry-kind" aria-hidden="true">${kindLabel}</span>`
+        ];
         if (part.plannedText){
           pieces.push(`<span class="time-entry-plan">${escapeHTML(part.plannedText)}</span>`);
         } else if (!part.showRealtime){
@@ -9463,11 +9466,17 @@ const mapContainerEl = map.getContainer();
               );
           const diffLabel = formatDelayDiff(part.diffSec);
           const realtimeText = part.realtimeText ?? part.plannedText ?? '—';
-          pieces.push(`<span class="time-entry-rt ${rtClass}">${escapeHTML(prefix)} ${escapeHTML(realtimeText)}${diffLabel ? ` (${escapeHTML(diffLabel)})` : ''}</span>`);
+          pieces.push('<span class="time-entry-arrow" aria-hidden="true">→</span>');
+          pieces.push(`<span class="time-entry-rt ${rtClass}" title="${escapeAttr(prefix)}">${part.estimated ? '~' : ''}${escapeHTML(realtimeText)}</span>`);
+          if (diffLabel){
+            pieces.push(`<span class="time-entry-diff">${escapeHTML(diffLabel)}</span>`);
+          }
         } else if (!part.plannedText && part.realtimeText){
+          pieces.push('<span class="time-entry-plan">—</span>');
+          pieces.push('<span class="time-entry-arrow" aria-hidden="true">→</span>');
           pieces.push(`<span class="time-entry-rt time-entry-rt--ontime">${escapeHTML(part.realtimeText)}</span>`);
         }
-        if (!pieces.length){
+        if (pieces.length === 1){
           pieces.push(`<span class="time-entry-plan">${escapeHTML(part.plannedText ?? '—')}</span>`);
         }
         const entryClasses = ['time-entry', cls];
@@ -20856,4 +20865,187 @@ html body.ber-marker-arrow-mode .leaflet-marker-pane .cow-marker .ber-train-arro
 </style>
 <!-- BER TRON TRAFFIC CLARITY V1 END -->
 
+<!-- BER CLEAN TRAIN SHEET + COLORED ARROWS V1 START -->
+<style id="ber-clean-train-sheet-colored-arrows-v1">
+/* Chronologie lisible : une colonne horaire fixe et une colonne gare. */
+.trip-panel:not(.station-board-mode) .trip-stops {
+  gap:2px !important;
+}
+.trip-panel:not(.station-board-mode) .trip-stop {
+  display:grid !important;
+  grid-template-columns:116px minmax(0,1fr) !important;
+  column-gap:10px !important;
+  align-items:start !important;
+  min-height:34px !important;
+  padding:3px 4px 4px 18px !important;
+  border-bottom:1px solid rgba(93,153,180,.10) !important;
+}
+.trip-panel:not(.station-board-mode) .trip-stop:last-child {
+  border-bottom:0 !important;
+}
+.trip-panel:not(.station-board-mode) .trip-stop::before {
+  left:2px !important;
+  top:9px !important;
+  width:9px !important;
+  height:9px !important;
+  border:1px solid rgba(236,254,255,.82) !important;
+  box-shadow:none !important;
+}
+.trip-panel:not(.station-board-mode) .trip-stop.current::before,
+.trip-panel:not(.station-board-mode) .trip-stop.enroute::before {
+  box-shadow:0 0 0 2px rgba(0,240,255,.18),0 0 6px rgba(0,240,255,.62) !important;
+}
+.trip-panel:not(.station-board-mode) .trip-stop:not(:last-child)::after {
+  left:6px !important;
+  top:19px !important;
+  bottom:-7px !important;
+  width:1px !important;
+}
+.trip-panel:not(.station-board-mode) .stop-time {
+  display:flex !important;
+  flex-direction:column !important;
+  align-items:stretch !important;
+  gap:2px !important;
+  min-width:0 !important;
+  width:116px !important;
+  color:#b8cad8 !important;
+  font-variant-numeric:tabular-nums !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry {
+  display:grid !important;
+  grid-template-columns:12px 31px 10px 33px minmax(0,1fr) !important;
+  align-items:baseline !important;
+  column-gap:2px !important;
+  min-height:12px !important;
+  margin:0 !important;
+  padding:0 !important;
+  line-height:1 !important;
+  white-space:nowrap !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry-kind {
+  color:#66889d !important;
+  font-size:7px !important;
+  font-weight:900 !important;
+  text-align:left !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry-plan {
+  display:block !important;
+  color:#c4d3dd !important;
+  font-size:9px !important;
+  font-weight:650 !important;
+  text-align:right !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry:has(.time-entry-rt--delay) .time-entry-plan,
+.trip-panel:not(.station-board-mode) .time-entry:has(.time-entry-rt--advance) .time-entry-plan {
+  color:#738795 !important;
+  text-decoration:line-through !important;
+  text-decoration-thickness:1px !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry-arrow {
+  color:#4c7188 !important;
+  font-size:8px !important;
+  text-align:center !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry-rt {
+  display:block !important;
+  font-size:9px !important;
+  font-weight:900 !important;
+  line-height:1 !important;
+  text-align:right !important;
+  opacity:1 !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry-rt--delay,
+.trip-panel:not(.station-board-mode) .time-entry-diff {
+  color:#ff9f1c !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry-rt--advance {
+  color:#62e6a6 !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry-rt--ontime {
+  color:#69edf7 !important;
+}
+.trip-panel:not(.station-board-mode) .time-entry-diff {
+  overflow:hidden !important;
+  font-size:7px !important;
+  font-weight:850 !important;
+  text-overflow:ellipsis !important;
+}
+.trip-panel:not(.station-board-mode) .stop-main {
+  min-width:0 !important;
+  padding-top:1px !important;
+}
+.trip-panel:not(.station-board-mode) .stop-name {
+  color:#eafaff !important;
+  font-size:11px !important;
+  font-weight:760 !important;
+  line-height:1.08 !important;
+}
+.trip-panel:not(.station-board-mode) .stop-note {
+  margin-top:2px !important;
+  font-size:7px !important;
+  line-height:1 !important;
+  letter-spacing:.035em !important;
+}
+.trip-panel:not(.station-board-mode) .stop-note strong {
+  color:#a0ff00 !important;
+  font-weight:850 !important;
+}
+.trip-panel:not(.station-board-mode) .trip-stop.cancelled .stop-note strong,
+.trip-panel:not(.station-board-mode) .trip-stop.cancelled .time-entry-diff {
+  color:#ff4164 !important;
+}
+.trip-panel:not(.station-board-mode) .trip-stop.cancelled .stop-name,
+.trip-panel:not(.station-board-mode) .trip-stop.cancelled .time-entry-plan,
+.trip-panel:not(.station-board-mode) .trip-stop.cancelled .time-entry-rt {
+  color:#ff6b83 !important;
+  text-decoration:line-through !important;
+  text-decoration-thickness:1px !important;
+}
+
+/* Le contour intermédiaire prend la couleur vive de l'opérateur. */
+html body.ber-marker-arrow-mode
+.leaflet-marker-pane
+.cow-marker
+.ber-train-arrow-white {
+  stroke:var(--ber-live-train-color,#7cff00) !important;
+  stroke-width:var(--ber-train-white-width) !important;
+  stroke-opacity:1 !important;
+}
+html body.ber-marker-arrow-mode
+.leaflet-marker-pane
+.cow-marker
+.ber-train-arrow-outer {
+  stroke:#01070d !important;
+  stroke-opacity:1 !important;
+}
+/* Reflet froid très fin : relief sans refaire un large contour blanc. */
+html body.ber-marker-arrow-mode
+.leaflet-marker-pane
+.cow-marker
+.ber-train-arrow-svg {
+  filter:
+    drop-shadow(0 -1px 0 rgba(225,253,255,.72))
+    drop-shadow(0 1px 0 var(--ber-live-train-color,#7cff00))
+    drop-shadow(0 3px 1px rgba(0,4,9,.96))
+    !important;
+}
+
+@media(max-width:720px){
+  .trip-panel:not(.station-board-mode) .trip-stop {
+    grid-template-columns:108px minmax(0,1fr) !important;
+    column-gap:7px !important;
+    min-height:31px !important;
+    padding-top:2px !important;
+    padding-bottom:3px !important;
+  }
+  .trip-panel:not(.station-board-mode) .stop-time {
+    width:108px !important;
+  }
+  .trip-panel:not(.station-board-mode) .time-entry {
+    grid-template-columns:11px 29px 8px 31px minmax(0,1fr) !important;
+  }
+}
+</style>
+<!-- BER CLEAN TRAIN SHEET + COLORED ARROWS V1 END -->
+
 </html>
