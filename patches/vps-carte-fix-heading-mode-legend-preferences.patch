--- a/map-v2/public/carte-core-preview.html
+++ b/map-v2/public/carte-core-preview.html
@@ -1039,12 +1039,12 @@
       <button id="legend-close" class="map-sheet-close" type="button" aria-label="Fermer la légende">×</button>
     </div>
     <div class="map-legend-grid">
-      <div><i class="map-train-swatch map-train-sncf"></i><span><strong>TER / SNCF</strong><small>Bleu</small></span></div>
-      <div><i class="map-train-swatch map-train-cfl"></i><span><strong>CFL</strong><small>Rose</small></span></div>
-      <div><i class="map-train-swatch map-train-tgv"></i><span><strong>TGV</strong><small>Or</small></span></div>
+      <div><svg class="map-legend-train-svg map-train-sncf" viewBox="0 0 24 22" aria-hidden="true"><path class="map-arrow-outer" d="M12 1 L22 20 L12 15.8 L2 20 Z"></path><path class="map-arrow-white" d="M12 1 L22 20 L12 15.8 L2 20 Z"></path><path class="map-arrow-core" d="M12 1 L22 20 L12 15.8 L2 20 Z"></path></svg><i class="map-legend-number map-number-sncf">88503</i><span><strong>TER / SNCF</strong><small>Bleu</small></span></div>
+      <div><svg class="map-legend-train-svg map-train-cfl" viewBox="0 0 24 22" aria-hidden="true"><path class="map-arrow-outer" d="M12 1 L22 20 L12 15.8 L2 20 Z"></path><path class="map-arrow-white" d="M12 1 L22 20 L12 15.8 L2 20 Z"></path><path class="map-arrow-core" d="M12 1 L22 20 L12 15.8 L2 20 Z"></path></svg><i class="map-legend-number map-number-cfl">RE11</i><span><strong>CFL</strong><small>Rouge</small></span></div>
+      <div><svg class="map-legend-train-svg map-train-tgv" viewBox="0 0 24 22" aria-hidden="true"><path class="map-arrow-outer" d="M12 1 L22 20 L12 15.8 L2 20 Z"></path><path class="map-arrow-white" d="M12 1 L22 20 L12 15.8 L2 20 Z"></path><path class="map-arrow-core" d="M12 1 L22 20 L12 15.8 L2 20 Z"></path></svg><i class="map-legend-number map-number-tgv">TGV</i><span><strong>TGV</strong><small>Doré</small></span></div>
       <div><i class="map-delay-swatch">+8 min</i><span><strong>Retard</strong><small>Temps estimé</small></span></div>
-      <div><i class="map-station-swatch map-station-main"></i><span><strong>Gare principale</strong><small>Repère pointé</small></span></div>
-      <div><i class="map-station-swatch map-station-small"></i><span><strong>Gare secondaire</strong><small>Petit rond</small></span></div>
+      <div><svg class="map-legend-station-main" viewBox="0 0 34 42" aria-hidden="true"><path class="station-pin-halo" d="M17 1.5C8.5 1.5 2.5 7.8 2.5 16.2c0 11.5 14.5 24.3 14.5 24.3s14.5-12.8 14.5-24.3C31.5 7.8 25.5 1.5 17 1.5Z"/><path class="station-pin-body" d="M17 3.8C9.9 3.8 4.8 9 4.8 16.2c0 8.5 8.8 18.2 12.2 21.5 3.4-3.3 12.2-13 12.2-21.5C29.2 9 24.1 3.8 17 3.8Z"/><path class="station-pin-roof" d="M9.5 14.2 17 9l7.5 5.2"/><path class="station-pin-building" d="M11.4 14.1h11.2v9.1H11.4z"/><path class="station-pin-door" d="M15 18h4v5.2h-4z"/><path class="station-pin-rail" d="M9.2 25.9h15.6M11 29h12M12.5 25.9 11 29m10.5-3.1L23 29"/></svg><span><strong>Gare principale</strong><small>Repère pointé</small></span></div>
+      <div><svg class="map-legend-station-small" viewBox="0 0 16 16" aria-hidden="true"><circle class="station-dot-halo" cx="8" cy="8" r="6.5"/><circle class="station-dot-body" cx="8" cy="8" r="3.8"/></svg><span><strong>Gare secondaire</strong><small>Petit rond</small></span></div>
     </div>
     <details class="map-lines-legend">
       <summary>Lignes ferroviaires</summary>
@@ -4971,6 +4971,27 @@
   const routesById = new Map();             // (optionnel) route_id -> {route_short_name, route_long_name}
   let activeServiceIds = null;              // Set<string> services actifs aujourd'hui (calendar/calendar_dates)
 
+  // ---------- Préférences locales de la carte ----------
+  const BER_MAP_PREFS_KEY = 'ber_map_preferences_v1';
+
+  function berReadMapPreferences(){
+    try {
+      const value = JSON.parse(localStorage.getItem(BER_MAP_PREFS_KEY) || '{}');
+      return value && typeof value === 'object' ? value : {};
+    } catch(_){
+      return {};
+    }
+  }
+
+  let berMapPreferences = berReadMapPreferences();
+
+  function berSaveMapPreferences(values){
+    berMapPreferences = { ...berMapPreferences, ...values };
+    try {
+      localStorage.setItem(BER_MAP_PREFS_KEY, JSON.stringify(berMapPreferences));
+    } catch(_) {}
+  }
+
   // ---------- Leaflet ----------
   const map = L.map('map', { preferCanvas:true }).setView([49.2, 6.15], 9);
   const lbMapStandard = L.tileLayer(
@@ -5019,7 +5040,18 @@
     }
   );
 
-  lbMapStandard.addTo(map);
+  const lbBaseLayersByPreference = {
+    standard:lbMapStandard,
+    faded:lbMapFaded,
+    bw:lbMapBW,
+    satellite:lbMapSatellite,
+    satelliteFaded:lbMapSatelliteFaded
+  };
+  const lbBaseLayerPreferenceKeys = new Map(
+    Object.entries(lbBaseLayersByPreference).map(([key,layer])=>[layer,key])
+  );
+  const lbInitialBaseLayer = lbBaseLayersByPreference[berMapPreferences.baseLayer] || lbMapFaded;
+  lbInitialBaseLayer.addTo(map);
 
   const lbBaseLayersControl = L.control.layers({
     'Plan standard': lbMapStandard,
@@ -5032,6 +5064,11 @@
     collapsed:true
   }).addTo(map);
 
+  map.on('baselayerchange', event=>{
+    const key = lbBaseLayerPreferenceKeys.get(event.layer);
+    if (key) berSaveMapPreferences({ baseLayer:key });
+  });
+
   const zoomControlContainer = map.zoomControl?.getContainer?.();
   const zoomInButton = zoomControlContainer?.querySelector('.leaflet-control-zoom-in');
   const zoomOutButton = zoomControlContainer?.querySelector('.leaflet-control-zoom-out');
@@ -5105,6 +5142,8 @@
   const trainCowById = new Map();
   const trainDataById = new Map();
   const trainMotionById = new Map();
+  // Orientation indépendante du DOM : un clic ne doit jamais remettre les flèches à 0°.
+  const trainHeadingById = new Map();
   // Une collecte peut omettre brièvement un train entre deux instantanés.
   // Ne jamais fermer immédiatement une fiche que l'utilisateur vient d'ouvrir.
   const SELECTED_TRAIN_MISSING_GRACE_MS = 30000;
@@ -5605,6 +5644,27 @@
   const controlsPanelEl = document.getElementById('controls-panel');
   const controlsCloseEl = document.getElementById('controls-close');
 
+  const berPreferenceCheckboxes = {
+    stationsMajor:toggleMajorEl,
+    stationsRegular:toggleRegularEl,
+    trains:toggleTrainsEl,
+    ter:toggleTerEl,
+    tgv:toggleTgvEl,
+    cfl:toggleCflEl,
+    delays:toggleDelaysEl,
+    smooth:document.getElementById('smooth')
+  };
+  for (const [key,input] of Object.entries(berPreferenceCheckboxes)){
+    if (input && typeof berMapPreferences[key] === 'boolean'){
+      input.checked = berMapPreferences[key];
+    }
+  }
+  const berPreferredPeriod = Number(berMapPreferences.period);
+  const berPeriodInput = document.getElementById('period');
+  if (berPeriodInput && Number.isFinite(berPreferredPeriod)){
+    berPeriodInput.value = String(Math.max(1,Math.min(30,Math.round(berPreferredPeriod))));
+  }
+
   const adaptiveLiteMode = (()=>{
     try {
       const lowThreads = Number(navigator.hardwareConcurrency || 0) > 0 && Number(navigator.hardwareConcurrency || 0) <= 4;
@@ -5620,8 +5680,8 @@
   function applyAdaptiveLiteDefaults(){
     if (!adaptiveLiteMode) return;
     document.body.classList.add('lite-ui');
-    if (toggleRegularEl && toggleRegularEl.checked) toggleRegularEl.checked = false;
-    if (toggleDelaysEl && !toggleDelaysEl.checked) toggleDelaysEl.checked = true;
+    if (toggleRegularEl && typeof berMapPreferences.stationsRegular !== 'boolean') toggleRegularEl.checked = false;
+    if (toggleDelaysEl && typeof berMapPreferences.delays !== 'boolean') toggleDelaysEl.checked = true;
   }
 
   applyAdaptiveLiteDefaults();
@@ -5652,6 +5712,7 @@
     layerVisibility.stationsMajor = toggleMajorEl.checked;
     toggleMajorEl.addEventListener('change', ()=>{
       layerVisibility.stationsMajor = toggleMajorEl.checked;
+      berSaveMapPreferences({ stationsMajor:toggleMajorEl.checked });
       applyLayerVisibility();
     });
   }
@@ -5660,6 +5721,7 @@
     layerVisibility.stationsRegular = toggleRegularEl.checked;
     toggleRegularEl.addEventListener('change', ()=>{
       layerVisibility.stationsRegular = toggleRegularEl.checked;
+      berSaveMapPreferences({ stationsRegular:toggleRegularEl.checked });
       applyLayerVisibility();
     });
   }
@@ -5668,6 +5730,7 @@
     layerVisibility.trains = toggleTrainsEl.checked;
     toggleTrainsEl.addEventListener('change', ()=>{
       layerVisibility.trains = toggleTrainsEl.checked;
+      berSaveMapPreferences({ trains:toggleTrainsEl.checked });
       applyLayerVisibility();
     });
   }
@@ -5681,22 +5744,22 @@
 
   if (toggleTerEl){
     trainFilters.showTer = toggleTerEl.checked;
-    toggleTerEl.addEventListener('change', ()=>{ trainFilters.showTer = toggleTerEl.checked; applyTrainFilters(); });
+    toggleTerEl.addEventListener('change', ()=>{ trainFilters.showTer = toggleTerEl.checked; berSaveMapPreferences({ ter:toggleTerEl.checked }); applyTrainFilters(); });
   }
 
   if (toggleTgvEl){
     trainFilters.showTgv = toggleTgvEl.checked;
-    toggleTgvEl.addEventListener('change', ()=>{ trainFilters.showTgv = toggleTgvEl.checked; applyTrainFilters(); });
+    toggleTgvEl.addEventListener('change', ()=>{ trainFilters.showTgv = toggleTgvEl.checked; berSaveMapPreferences({ tgv:toggleTgvEl.checked }); applyTrainFilters(); });
   }
 
   if (toggleCflEl){
     trainFilters.showCfl = toggleCflEl.checked;
-    toggleCflEl.addEventListener('change', ()=>{ trainFilters.showCfl = toggleCflEl.checked; applyTrainFilters(); });
+    toggleCflEl.addEventListener('change', ()=>{ trainFilters.showCfl = toggleCflEl.checked; berSaveMapPreferences({ cfl:toggleCflEl.checked }); applyTrainFilters(); });
   }
 
   if (toggleDelaysEl){
     trainFilters.showDelays = toggleDelaysEl.checked;
-    toggleDelaysEl.addEventListener('change', ()=>{ trainFilters.showDelays = toggleDelaysEl.checked; applyTrainFilters(); });
+    toggleDelaysEl.addEventListener('change', ()=>{ trainFilters.showDelays = toggleDelaysEl.checked; berSaveMapPreferences({ delays:toggleDelaysEl.checked }); applyTrainFilters(); });
   }
 
   function toggleInfoPanel(force){
@@ -8264,12 +8327,12 @@
     const titleAttr = (trainFilters.showDelays && delayInfo?.title) ? ` title="${escapeAttr(delayInfo.title)}"` : '';
     const classNames = ['cow-marker'];
 
-    const initialHeading =
-      Number(
-        train?.bearing
-        ?? train?.heading
-        ?? train?.course
-      );
+    const initialHeading = Number(
+      trainHeadingById.get(String(train?.id))
+      ?? train?.bearing
+      ?? train?.heading
+      ?? train?.course
+    );
 
     if (!Number.isFinite(initialHeading)){
       classNames.push(
@@ -12411,6 +12474,10 @@
     marker._berHeading =
       heading;
 
+    if (train?.id != null){
+      trainHeadingById.set(String(train.id), heading);
+    }
+
 
     /*
      * Une simple variable CSS :
@@ -12665,6 +12732,7 @@
         trainCowById.delete(id);
         trainDataById.delete(id);
         trainMotionById.delete(id);
+        trainHeadingById.delete(String(id));
         if (activeTripId === id){
           activeTripId = null;
           if (activeStationId){
@@ -12795,10 +12863,10 @@
   // ---------- Boucle ----------
   let periodMs = 10000, nextTick = performance.now();
   const periodEl = document.getElementById('period'), hzEl=document.getElementById('hz'), smoothEl=document.getElementById('smooth');
-  if (adaptiveLiteMode && smoothEl && smoothEl.checked){
+  if (adaptiveLiteMode && smoothEl && typeof berMapPreferences.smooth !== 'boolean' && smoothEl.checked){
     smoothEl.checked = false;
   }
-  if (adaptiveLiteMode && periodEl && Number(periodEl.value) < 12){
+  if (adaptiveLiteMode && periodEl && !Number.isFinite(Number(berMapPreferences.period)) && Number(periodEl.value) < 12){
     periodEl.value = '12';
   }
   hzEl.textContent = periodEl.value;
@@ -12813,6 +12881,8 @@
       periodMs =
         Number(periodEl.value) * 1000;
 
+      berSaveMapPreferences({ period:Number(periodEl.value) });
+
       nextTick =
         performance.now();
 
@@ -12825,6 +12895,10 @@
     }
   );
 
+  smoothEl?.addEventListener('change', ()=>{
+    berSaveMapPreferences({ smooth:smoothEl.checked });
+  });
+
   // ==========================================================
   // BER V13.1 POSITION ONLY
   //
@@ -17526,11 +17600,16 @@
 
   function createControl(){
 
-    if (
-      document.getElementById(
-        'ber-train-marker-mode-control'
-      )
-    ){
+    const existingControl = document.getElementById('ber-train-marker-mode-control');
+    if (existingControl){
+      const existingSelect = existingControl.querySelector('#ber-train-marker-mode');
+      if (existingSelect){
+        existingSelect.value = readMode();
+        if (existingSelect.dataset.berModeBound !== '1'){
+          existingSelect.dataset.berModeBound = '1';
+          existingSelect.addEventListener('change', ()=>applyMode(existingSelect.value));
+        }
+      }
       return;
     }
 
@@ -18719,12 +18798,18 @@
 .map-legend-grid span{display:grid;min-width:0}
 .map-legend-grid strong{font-size:10px;color:#eafaff}
 .map-legend-grid small{font-size:8px;color:#83a4b6}
-.map-train-swatch{width:0;height:0;border-left:6px solid transparent;border-right:6px solid transparent;border-bottom:15px solid;filter:drop-shadow(0 0 2px #fff)}
-.map-train-sncf{border-bottom-color:#42b8ff}.map-train-cfl{border-bottom-color:#ff6f9f}.map-train-tgv{border-bottom-color:#f3d36d}
+.map-legend-train-svg{width:18px;height:18px;overflow:visible;flex:0 0 18px}
+.map-legend-train-svg .map-arrow-outer{fill:none;stroke:rgba(10,21,30,.94);stroke-width:4.4;stroke-linejoin:round;stroke-linecap:round}
+.map-legend-train-svg .map-arrow-white{fill:none;stroke:#fff;stroke-width:2.8;stroke-linejoin:round;stroke-linecap:round}
+.map-legend-train-svg .map-arrow-core{stroke:none}
+.map-train-sncf .map-arrow-core{fill:#1b3b73}.map-train-cfl .map-arrow-core{fill:#601020}.map-train-tgv .map-arrow-core{fill:#8f7531}
+.map-legend-number{display:none;padding:3px 4px;border-radius:4px;color:#fff;font:800 8px system-ui,sans-serif;font-style:normal;white-space:nowrap}
+.map-number-sncf{background:rgba(18,38,73,.92);color:#9fc3ff}.map-number-cfl{background:rgba(96,16,32,.92);color:#ffd6d9}.map-number-tgv{background:rgba(70,52,12,.94);color:#fff0c2}
+body:not(.ber-marker-arrow-mode) .map-legend-train-svg{display:none}
+body:not(.ber-marker-arrow-mode) .map-legend-number{display:inline-flex}
 .map-delay-swatch{padding:2px 4px;border-radius:5px;background:#ffbd5c;color:#182334;font:900 8px system-ui,sans-serif}
-.map-station-swatch{display:inline-block;box-sizing:border-box}
-.map-station-main{width:16px;height:20px;border:2px solid #f7fbff;border-radius:9px 9px 9px 2px;background:#07314a;transform:rotate(-45deg)}
-.map-station-small{width:12px;height:12px;border:2px solid #fff;border-radius:50%;background:#31e7f2;box-shadow:inset 0 0 0 2px #07314a}
+.map-legend-station-main{width:19px;height:24px;overflow:visible;flex:0 0 19px}
+.map-legend-station-small{width:14px;height:14px;overflow:visible;flex:0 0 14px;margin:0 2px}
 .map-lines-legend{margin-top:9px;padding-top:9px;border-top:1px solid rgba(111,178,201,.18)}
 .map-lines-legend > div{display:flex;align-items:center;gap:7px;margin-top:6px;font-size:10px;color:#a9c1ce}
 .map-about-sheet{width:min(440px,calc(100vw - 20px))!important}
@@ -18741,6 +18826,14 @@
 .leaflet-control-zoom a,.leaflet-control-layers-toggle{background-color:rgba(3,14,25,.94)!important;color:#e8fbff!important;border-color:rgba(83,211,235,.28)!important}
 .leaflet-control-zoom,.leaflet-control-layers{border:1px solid rgba(83,211,235,.32)!important;border-radius:10px!important;overflow:hidden;box-shadow:0 5px 16px rgba(0,0,0,.34)!important}
 .leaflet-control-layers-expanded{background:rgba(3,14,25,.96)!important;color:#e8fbff!important;font-size:10px;border-radius:10px}
+/* La sélection ne doit jamais remplacer rotate(--ber-heading) par un simple scale(). */
+html body.ber-marker-arrow-mode.train-trip-open .leaflet-marker-pane .cow-marker .cow-glyph{
+  transform:translate(-50%,-50%) rotate(var(--ber-heading,0deg))!important;
+}
+html body.ber-marker-arrow-mode.train-trip-open .leaflet-marker-pane .cow-marker.train-selected .cow-glyph,
+html body.ber-marker-arrow-mode.train-trip-open .leaflet-marker-pane .cow-marker.ber-focus-current .cow-glyph{
+  transform:translate(-50%,-50%) rotate(var(--ber-heading,0deg)) scale(1.08)!important;
+}
 @media(max-width:720px){
   .map-tool-dock{bottom:max(8px,env(safe-area-inset-bottom));padding:4px}
   .map-tool-dock .fab-button{min-height:32px;padding:5px 8px;font-size:9px}

