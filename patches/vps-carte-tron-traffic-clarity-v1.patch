diff --git a/map-v2/public/carte-core-preview.html b/map-v2/public/carte-core-preview.html
index 96b267bd4..c2c329154 100644
--- a/map-v2/public/carte-core-preview.html
+++ b/map-v2/public/carte-core-preview.html
@@ -8740,8 +8740,15 @@ function berDrawSelectedRouteProgress(){
       }
     }
     const aria = escapeAttr(ariaParts.join(', '));
+    /* Sur la carte, le signal reste très court. Le texte complet demeure dans
+       l'infobulle, l'ARIA et la fiche du train. */
+    const mapDelayLabel = delayInfo?.severity === 'cancelled'
+      ? 'SUP.'
+      : Number.isFinite(Number(delayInfo?.minutes))
+        ? `+${Math.round(Number(delayInfo.minutes))}`
+        : (delayInfo?.label || '');
     const badge = (trainFilters.showDelays && delayInfo)
-      ? `<span class="${delayInfo.badgeClass}"${delayInfo.title ? ` title="${escapeAttr(delayInfo.title)}"` : ''}>${escapeHTML(delayInfo.label)}</span>`
+      ? `<span class="${delayInfo.badgeClass}"${delayInfo.title ? ` title="${escapeAttr(delayInfo.title)}"` : ''}>${escapeHTML(mapDelayLabel)}</span>`
       : '';
     const titleAttr = (trainFilters.showDelays && delayInfo?.title) ? ` title="${escapeAttr(delayInfo.title)}"` : '';
     const classNames = ['cow-marker'];
@@ -9293,7 +9300,11 @@ const mapContainerEl = map.getContainer();
       const theoreticalTerminus = stops[stops.length - 1]?.name || 'le terminus prévu';
       summaryHtml += `<div class="trip-panel-delay delay-cancelled">${escapeHTML(`Suppression partielle · terminus exceptionnel à ${endName} (supprimé jusqu’à ${theoreticalTerminus})`)}</div>`;
     }
-    if (renderedDelayInfo){
+    /* Un retard simple est déjà expliqué par la ligne Temps réel, les horaires
+       recalculés arrêt par arrêt et la cause éventuelle. Ne pas répéter ici
+       « Retard +35 min à Pagny-sur-Moselle ». Les suppressions conservent leur
+       avertissement dédié car elles modifient la desserte. */
+    if (renderedDelayInfo?.severity === 'cancelled'){
       const severityClass =
         ['moderate','major','severe','cancelled'].includes(renderedDelayInfo.severity)
           ? ` delay-${renderedDelayInfo.severity}`
@@ -20745,4 +20756,104 @@ html body.ber-marker-arrow-mode
 </style>
 <!-- BER TRON CRISP MAP LAYERS V1 END -->
 
+<!-- BER TRON TRAFFIC CLARITY V1 START -->
+<style id="ber-tron-traffic-clarity-v1">
+/* Aucun fond carré sous les gares principales, quel que soit le mode. */
+.leaflet-marker-pane .leaflet-marker-icon:has(.station-marker),
+.leaflet-marker-pane .leaflet-marker-icon:has(.station-marker)::before,
+.leaflet-marker-pane .leaflet-marker-icon:has(.station-marker)::after,
+.leaflet-marker-pane .station-marker,
+.leaflet-marker-pane .station-marker::before,
+.leaflet-marker-pane .station-marker::after {
+  background:transparent !important;
+  background-color:transparent !important;
+  background-image:none !important;
+  border:0 !important;
+  border-radius:0 !important;
+  outline:0 !important;
+  box-shadow:none !important;
+  appearance:none !important;
+  -webkit-appearance:none !important;
+}
+.leaflet-marker-pane .leaflet-marker-icon:has(.station-marker)::before,
+.leaflet-marker-pane .leaflet-marker-icon:has(.station-marker)::after,
+.leaflet-marker-pane .station-marker::after {
+  content:none !important;
+  display:none !important;
+}
+/* La zone tactile transparente reste disponible sans générer de plaque. */
+.leaflet-marker-pane .station-marker::before {
+  content:"" !important;
+  display:block !important;
+  opacity:0 !important;
+}
+
+/* Badges trafic #BER : compacts, francs, sans halo diffus. */
+html body .leaflet-marker-pane .cow-marker .train-delay-badge {
+  min-width:0 !important;
+  min-height:0 !important;
+  margin:0 !important;
+  padding:2px 4px !important;
+  border:1px solid rgba(255,255,255,.88) !important;
+  border-radius:5px !important;
+  font-size:8px !important;
+  font-weight:900 !important;
+  line-height:1 !important;
+  letter-spacing:0 !important;
+  text-transform:uppercase !important;
+  box-shadow:0 1px 0 #07101a,0 2px 2px rgba(0,0,0,.72) !important;
+}
+html body .leaflet-marker-pane .cow-marker .train-delay-badge--moderate {
+  color:#07111c !important;
+  background:#ffd166 !important;
+  border-color:#fff1bd !important;
+}
+html body .leaflet-marker-pane .cow-marker .train-delay-badge--major {
+  color:#08111a !important;
+  background:#ff9f1c !important;
+  border-color:#ffe0a3 !important;
+}
+html body .leaflet-marker-pane .cow-marker .train-delay-badge--severe {
+  color:#07111c !important;
+  background:#ff7a00 !important;
+  border-color:#ffd09a !important;
+}
+html body .leaflet-marker-pane .cow-marker .train-delay-badge--cancelled {
+  color:#fff !important;
+  background:#ff1744 !important;
+  border-color:#ffd7df !important;
+}
+
+/* Vue régionale : la couleur signale l'incident sans mur d'étiquettes. */
+html.ber-arrow-z-far body .leaflet-marker-pane .cow-marker .train-delay-badge,
+html.ber-arrow-z-wide body .leaflet-marker-pane .cow-marker .train-delay-badge {
+  width:8px !important;
+  height:8px !important;
+  padding:0 !important;
+  overflow:hidden !important;
+  border-width:1px !important;
+  border-radius:50% !important;
+  color:transparent !important;
+  font-size:0 !important;
+  transform:translate(9px,-9px) !important;
+}
+html.ber-arrow-z-far body .leaflet-marker-pane .cow-marker .train-delay-badge--cancelled,
+html.ber-arrow-z-wide body .leaflet-marker-pane .cow-marker .train-delay-badge--cancelled {
+  border-radius:2px !important;
+}
+
+/* Les trains restent la couche vivante la plus haute et la plus nette. */
+.leaflet-marker-pane .leaflet-marker-icon:has(.cow-marker) {
+  z-index:1000 !important;
+}
+html body.ber-marker-arrow-mode .leaflet-marker-pane .cow-marker .ber-train-arrow-svg {
+  filter:
+    drop-shadow(0 -1px 0 rgba(255,255,255,.98))
+    drop-shadow(0 1px 0 var(--ber-live-train-color,#7cff00))
+    drop-shadow(0 3px 1px rgba(0,4,9,.96))
+    !important;
+}
+</style>
+<!-- BER TRON TRAFFIC CLARITY V1 END -->
+
 </html>
