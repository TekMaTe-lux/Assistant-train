diff --git a/map-v2/public/carte-core-preview.html b/map-v2/public/carte-core-preview.html
index 69c74ac0e..96b267bd4 100644
--- a/map-v2/public/carte-core-preview.html
+++ b/map-v2/public/carte-core-preview.html
@@ -5804,7 +5804,8 @@ function sanitizeCflStationName(name){
 
   // Couches temporaires de la circulation sélectionnée.
   map.createPane('selectedTripRoutePane');
-  map.getPane('selectedTripRoutePane').style.zIndex = '460';
+  /* Au-dessus des lignes réseau, sous les marqueurs et les gares du trajet. */
+  map.getPane('selectedTripRoutePane').style.zIndex = '590';
   map.getPane('selectedTripRoutePane').style.pointerEvents = 'none';
   map.createPane('selectedTripStopsPane');
   map.getPane('selectedTripStopsPane').style.zIndex = '640';
@@ -7244,11 +7245,11 @@ function berDrawSelectedRouteProgress(){
         {
           pane:'selectedTripRoutePane',
 
-          color:'#303942',
+          color:'#d8fbff',
 
-          weight:6,
+          weight:8,
 
-          opacity:.72,
+          opacity:.76,
 
           lineCap:'round',
 
@@ -7271,9 +7272,9 @@ function berDrawSelectedRouteProgress(){
 
           color:'#087cff',
 
-          weight:3.5,
+          weight:4.5,
 
-          opacity:.62,
+          opacity:.74,
 
           lineCap:'round',
 
@@ -7307,17 +7308,19 @@ function berDrawSelectedRouteProgress(){
         {
           pane:'selectedTripRoutePane',
 
-          color:'#061522',
+          color:'#eaffff',
 
-          weight:7,
+          weight:9,
 
-          opacity:.76,
+          opacity:.96,
 
           lineCap:'round',
 
           lineJoin:'round',
 
-          interactive:false
+          interactive:false,
+
+          className:'ber-route-future-casing'
         }
       )
       .addTo(
@@ -7332,7 +7335,7 @@ function berDrawSelectedRouteProgress(){
 
           color:'#087cff',
 
-          weight:4.5,
+          weight:5.25,
 
           opacity:1,
 
@@ -7340,7 +7343,9 @@ function berDrawSelectedRouteProgress(){
 
           lineJoin:'round',
 
-          interactive:false
+          interactive:false,
+
+          className:'ber-route-future-core'
         }
       )
       .addTo(
@@ -8479,7 +8484,11 @@ function berDrawSelectedRouteProgress(){
         { lat:route[i-1][0], lon:route[i-1][1] },
         { lat:route[i][0], lon:route[i][1] }
       );
-      if (!Number.isFinite(edge) || edge > RAIL_MAX_EDGE_M) return null;
+      /* Certaines shapes officielles sont simplement moins densément
+         échantillonnées. Jusqu'à 2,2 km, l'ordre et le passage près de toutes
+         les gares restent les contrôles autoritaires. */
+      const selectedRouteMaxEdge = Math.max(RAIL_MAX_EDGE_M, 2200);
+      if (!Number.isFinite(edge) || edge > selectedRouteMaxEdge) return null;
       cumulative.push(cumulative[i-1] + edge);
     }
 
@@ -8593,6 +8602,23 @@ function berDrawSelectedRouteProgress(){
         focusSelectedRoute(validatedCoords);
         return;
       }
+
+      /* Une shape V2 peut être ferroviairement correcte tout en contenant des
+         points espacés de plus de 800 m. Si la réparation par le graphe local
+         échoue, on conserve cette source uniquement après validation complète
+         des gares, de leur ordre et des distances du parcours. */
+      const rawV2Coords = Array.isArray(v2Path?.geometry?.coordinates)
+        ? v2Path.geometry.coordinates
+            .map(coord => [Number(coord?.[1]), Number(coord?.[0])])
+            .filter(coord => coord.every(Number.isFinite))
+        : [];
+      const validatedRawV2Coords = validateSelectedRouteCorridor(rawV2Coords, seq);
+      if (validatedRawV2Coords){
+        selectedTripRailPathCache.set(routeTripId, validatedRawV2Coords);
+        addSelectedLatLonPath(validatedRawV2Coords);
+        focusSelectedRoute(validatedRawV2Coords);
+        return;
+      }
     }
 
     /* Secours strict : assembler une seule route, jamais 15 segments écrasés. */
@@ -20647,8 +20673,8 @@ html.ber-arrow-z-axis {
   stroke-width:2.15 !important;
 }
 .leaflet-marker-pane .station-marker.station-regular .station-dot-body {
-  fill:#087cff !important;
-  stroke:#64f3ff !important;
+  fill:#55e8f5 !important;
+  stroke:#063044 !important;
   stroke-width:1.15 !important;
 }
 
