--- a/map-v2/public/carte-core-preview.html
+++ b/map-v2/public/carte-core-preview.html
@@ -5216,6 +5216,54 @@
     if (newestCtx) merged.context.current_datetime = newestCtx;
 
     const parsed = parseSncfRealtimeData(merged);
+    // Les statuts précis des impacted_stops vivent dans les disruptions du HUB,
+    // pas dans vehicle_journey.stop_times. On reconstruit ici le parcours
+    // réellement assuré avant de publier la source SNCF à la carte.
+    for (const [num, item] of Object.entries(hub.trains || {})){
+      const data = item?.data;
+      if (!item?.ok || !data) continue;
+      const trainKey = String(normalizeTrainNumberKey(num) || num);
+      const orderedImpacts = [];
+      for (const disruption of data.disruptions || []){
+        for (const object of disruption?.impacted_objects || []){
+          for (const impact of object?.impacted_stops || []){
+            if (impact?.stop_point) orderedImpacts.push(impact);
+          }
+        }
+      }
+      if (!orderedImpacts.length) continue;
+
+      const firstDeparture = orderedImpacts.findIndex(impact => (
+        String(impact?.departure_status || '').toLowerCase() !== 'deleted'
+      ));
+      let lastArrival = -1;
+      for (let index = orderedImpacts.length - 1; index >= 0; index -= 1){
+        if (String(orderedImpacts[index]?.arrival_status || '').toLowerCase() !== 'deleted'){
+          lastArrival = index;
+          break;
+        }
+      }
+      if (firstDeparture < 0 || lastArrival < firstDeparture) continue;
+
+      let perStation = parsed.perTrain.get(trainKey);
+      if (!(perStation instanceof Map)) perStation = new Map();
+      orderedImpacts.forEach((impact, index) => {
+        const stopPoint = impact.stop_point || {};
+        const stationName = stopPoint.name || stopPoint.label || stopPoint.id;
+        if (!stationName) return;
+        const arrivalDeleted = String(impact.arrival_status || '').toLowerCase() === 'deleted';
+        const departureDeleted = String(impact.departure_status || '').toLowerCase() === 'deleted';
+        const outsideService = index < firstDeparture || index > lastArrival;
+        if (!outsideService && !(arrivalDeleted && departureDeleted)) return;
+        registerSncfStationEntry(
+          perStation,
+          stationName,
+          { stopPointId:stopPoint.id, stopAreaId:stopPoint.stop_area?.id },
+          null
+        );
+      });
+      if (perStation.size) parsed.perTrain.set(trainKey, perStation);
+    }
     if (directTrainDisruptions.size){
       if (!(parsed.disruptions instanceof Map)) parsed.disruptions = new Map();
       for (const [k, v] of directTrainDisruptions.entries()){
@@ -8459,7 +8507,19 @@
     const allRealtimeStopsCancelled = hasRealtimeStops && realtimeProfile.stops.length > 0
       ? realtimeProfile.stops.every(st => st?.cancelled)
       : false;
-    const isTrainCancelled = cancellationInfo ? cancellationInfo.index === 0 : delayInfo?.severity === 'cancelled';
+    const firstServedIndex = hasRealtimeStops
+      ? realtimeProfile.stops.findIndex(stop => !stop?.cancelled)
+      : -1;
+    let lastServedIndex = -1;
+    if (hasRealtimeStops){
+      for (let index = realtimeProfile.stops.length - 1; index >= 0; index -= 1){
+        if (!realtimeProfile.stops[index]?.cancelled){ lastServedIndex = index; break; }
+      }
+    }
+    const hasPartialStart = firstServedIndex > 0;
+    const hasPartialEnd = lastServedIndex >= 0 && lastServedIndex < seq.length - 1;
+    const isTrainCancelled = allRealtimeStopsCancelled
+      || (!hasPartialStart && !hasPartialEnd && delayInfo?.severity === 'cancelled');
     const propagateTrainCancellation = isTrainCancelled && (!hasRealtimeStops || allRealtimeStopsCancelled);
     
     tripPanelEl.classList.toggle('cancelled', Boolean(isTrainCancelled));
@@ -8516,13 +8576,9 @@
       };
     };
     
-    const partialCancellationIndex = (cancellationInfo && Number.isInteger(cancellationInfo.index) && cancellationInfo.index > 0)
-      ? cancellationInfo.index
-      : null;
-    const hasPartialTermination = partialCancellationIndex != null && partialCancellationIndex < seq.length;
-    const visibleStopIndices = hasPartialTermination
-      ? Array.from({ length: partialCancellationIndex }, (_, i)=>i)
-      : seq.map((_, i)=>i);
+    // Toujours conserver le parcours théorique complet dans le détail.
+    // Les gares non desservies restent visibles et sont marquées supprimées.
+    const visibleStopIndices = seq.map((_, i)=>i);
 
     const stops = visibleStopIndices.map((seqIdx, idx)=>{
       const st = seq[seqIdx];
@@ -8594,30 +8650,38 @@
         departureRealtime: departureRt,
         isFirst,
         isLast,
+        isNewOrigin: hasPartialStart && seqIdx === firstServedIndex,
+        isNewTerminus: hasPartialEnd && seqIdx === lastServedIndex,
         cancelled: stopCancelled,
         label,
         timeParts
       };
     });
 
-    const startName = stops[0]?.name || '—';
-    const endName = stops[stops.length - 1]?.name || '—';
-    const plannedStartSec = data.scheduledStartSec ?? realtimeProfile?.startPlanned ?? seq[0]?.departure ?? seq[0]?.arrival ?? null;
-    const displayLastSeqIdx = stops.length ? (stops[stops.length - 1].seqIdx ?? (seq.length - 1)) : (seq.length - 1);
-    const plannedEndSec = hasPartialTermination
-      ? (seq[displayLastSeqIdx]?.arrival ?? seq[displayLastSeqIdx]?.departure ?? null)
+    const effectiveStart = hasPartialStart ? stops[firstServedIndex] : stops[0];
+    const effectiveEnd = hasPartialEnd ? stops[lastServedIndex] : stops[stops.length - 1];
+    const startName = effectiveStart?.name || '—';
+    const endName = effectiveEnd?.name || '—';
+    const plannedStartSec = hasPartialStart
+      ? (seq[firstServedIndex]?.departure ?? seq[firstServedIndex]?.arrival ?? null)
+      : (data.scheduledStartSec ?? realtimeProfile?.startPlanned ?? seq[0]?.departure ?? seq[0]?.arrival ?? null);
+    const plannedEndSec = hasPartialEnd
+      ? (seq[lastServedIndex]?.arrival ?? seq[lastServedIndex]?.departure ?? null)
       : (data.scheduledEndSec ?? realtimeProfile?.endPlanned ?? seq[seq.length - 1]?.arrival ?? seq[seq.length - 1]?.departure ?? null);
     const summaryStartTime = plannedStartSec!=null ? fmtHHMM(plannedStartSec) : (stops[0]?.label ?? '—');
     const summaryEndTime = plannedEndSec!=null ? fmtHHMM(plannedEndSec) : (stops[stops.length - 1]?.label ?? '—');
     let summaryHtml = `${escapeHTML(startName)} <strong>${escapeHTML(summaryStartTime)}</strong> → ${escapeHTML(endName)} <strong>${escapeHTML(summaryEndTime)}</strong>`;
     let renderedDelayInfo = trainFilters.showDelays ? delayInfo : null;
-    if (renderedDelayInfo?.severity === 'cancelled' && cancellationInfo && cancellationInfo.index > 0){
+    if (renderedDelayInfo?.severity === 'cancelled' && (hasPartialStart || hasPartialEnd)){
       renderedDelayInfo = null;
     }
-    if (hasPartialTermination){
-      const terminalLabel = stops[stops.length - 1]?.name || 'cet arrêt';
-      const stationLabel = cancellationInfo?.station || 'l’arrêt suivant';
-      summaryHtml += `<div class="trip-panel-delay delay-cancelled">${escapeHTML(`Suppression partielle · terminus exceptionnel à ${terminalLabel} (supprimé à partir de ${stationLabel})`)}</div>`;
+    if (hasPartialStart){
+      const theoreticalOrigin = stops[0]?.name || 'la gare d’origine';
+      summaryHtml += `<div class="trip-panel-delay delay-cancelled">${escapeHTML(`Suppression partielle · départ exceptionnel à ${startName} (supprimé depuis ${theoreticalOrigin})`)}</div>`;
+    }
+    if (hasPartialEnd){
+      const theoreticalTerminus = stops[stops.length - 1]?.name || 'le terminus prévu';
+      summaryHtml += `<div class="trip-panel-delay delay-cancelled">${escapeHTML(`Suppression partielle · terminus exceptionnel à ${endName} (supprimé jusqu’à ${theoreticalTerminus})`)}</div>`;
     }
     if (renderedDelayInfo){
       const severityClass =
@@ -8640,9 +8704,11 @@
     const disruptionText = findSncfDisruptionForTrain(data);
     
     if (trainFilters.showDelays){
-      const realtimeStartSec = realtimeProfile?.startRealtime ?? data.startSec ?? plannedStartSec;
-      const realtimeEndSec = hasPartialTermination
-        ? (stops[stops.length - 1]?.arrivalRealtime ?? stops[stops.length - 1]?.departureRealtime ?? plannedEndSec)
+      const realtimeStartSec = hasPartialStart
+        ? (effectiveStart?.departureRealtime ?? effectiveStart?.arrivalRealtime ?? plannedStartSec)
+        : (realtimeProfile?.startRealtime ?? data.startSec ?? plannedStartSec);
+      const realtimeEndSec = hasPartialEnd
+        ? (effectiveEnd?.arrivalRealtime ?? effectiveEnd?.departureRealtime ?? plannedEndSec)
         : (realtimeProfile?.endRealtime ?? data.endSec ?? plannedEndSec);
       const startDiffText = (plannedStartSec!=null && realtimeStartSec!=null) ? formatDelayDiff(realtimeStartSec - plannedStartSec) : '';
       const endDiffText = (plannedEndSec!=null && realtimeEndSec!=null) ? formatDelayDiff(realtimeEndSec - plannedEndSec) : '';
@@ -8737,8 +8803,10 @@
       }
 
       const noteParts = [];
-      if (info.isFirst) noteParts.push('Départ');
-      if (info.isLast) noteParts.push('TERMINUS');
+      if (info.isNewOrigin) noteParts.push('DÉPART EXCEPTIONNEL');
+      else if (info.isFirst && !info.cancelled) noteParts.push('Départ');
+      if (info.isNewTerminus) noteParts.push('TERMINUS EXCEPTIONNEL');
+      else if (info.isLast && !info.cancelled) noteParts.push('TERMINUS');
       if (segIdx >= 0){
         if (idx === segIdx) noteParts.push('Arrêt précédent');
         if (idx === segIdx + 1) noteParts.push('Prochain arrêt');
